import LoginPage from "@/src/shared/log-in/LoginPage";

const page = () => {
  return (
    <div className="h-screen flex items-center justify-center rounded-8">
      <LoginPage />
    </div>
  );
};

export default page;
