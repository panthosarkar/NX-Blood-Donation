export const addOption = (id: any, title: any, value: any) => {
  return { id, title, value };
};

export const addOption2 = (id: any, logo: any, title: any, value: any) => {
  return { id, logo, title, value };
};
